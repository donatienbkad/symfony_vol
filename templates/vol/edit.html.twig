{% extends 'base.html.twig' %}

{% block title %}édition vol{% endblock %}

{% block body %}
<div class="container p-3">
    <h2 class="text-center mb-2">Editer Vol</h2>
    
    {{ include('vol/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="d-flex justify-content-evenly">
        <a href="{{ path('app_vol_index') }}" class="btn btn-success">Retour à la liste</a>
        {{ include('vol/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
